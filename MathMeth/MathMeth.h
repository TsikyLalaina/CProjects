#ifndef H_M
#define H_M
#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <math.h>

int hextoten(char input);
int toten(char* str, int input_base);
char* fromtento(char* str, int output_base);
char* convertbases(char* str, int base1, int base2);

#endif
